{
  "private": true,
  "name": "api",
  "version": "0.0.0",
  "scripts": {
    "build": "node scripts/prepare-assets.mjs",
    "start": "yarn build && wrangler dev",
    "deploy": "yarn build && wrangler deploy",
    "test": "vitest",
    "clean": "rm -rf ./.wrangler"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.19.8",
    "@hono/zod-validator": "^0.1.11",
    "@resvg/resvg-wasm": "^2.6.2",
    "@scalar/hono-api-reference": "^0.9.3",
    "chess.js": "^1.4.0",
    "hono": "^4.0.4",
    "postgres": "^3.4.5",
    "ts-pattern": "^5.0.8",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "vitest": "^3.2.6"
  }
}
